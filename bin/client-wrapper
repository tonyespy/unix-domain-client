#!/bin/sh

# set shell options:
#  -e 	    Exit immediately if a command exits w/non-zero status
#  -x	    print commands and their args as executed
set -ex

mkdir -p $SNAP_DATA/sockets

$SNAP/bin/unix-domain-client $SNAP_DATA
